# ── Secrets and local configuration ──────────────────────────────────
# None of this may ever reach a public repository.
.env
.env.*
!env.example
*.env
secrets.*
secrets/
credentials.*
config.local.*
*.local.json

# Keys and certificates
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*.crt
*.cer
id_rsa*
id_ed25519*
*.gpg
*.asc

# Cloud and tooling authentication
.netrc
.npmrc
.yarnrc
.pypirc
.aws/
.gcloud/
.azure/
.ssh/
.vercel
*.tfvars
*.tfstate
*.tfstate.*
.terraform/

# Supabase CLI local state (contains local database credentials)
supabase/.branches/
supabase/.temp/
supabase/config.toml

# Agent and editor configuration that can carry local paths
.claude/
.cursor/
.aider*
.mcp.json

# ── Dependencies and build output ────────────────────────────────────
node_modules/
.pnp
.pnp.*
.next/
out/
build/
dist/
.turbo/
.swc/
coverage/
*.tsbuildinfo
next-env.d.ts

# ── Logs and temporary files ─────────────────────────────────────────
*.log
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-debug.log*
*.pid
*.tmp
*.temp
*.bak
*.swp
*~
tmp/
scratch/
scratchpad/

# ── Data exports (may contain real player data) ──────────────────────
*.sqlite
*.sqlite3
*.db
*.dump
*.sql.gz
borsbraket-*.csv
export/
exports/

# scripts/verify-universe.ts writes this beside itself: one row per
# instrument saying what the price feed made of it. The run's log is the
# summary; this is the detail, and it belongs to the run, not the repo.
universe-report.json

# ── Operating system and editors ─────────────────────────────────────
.DS_Store
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini
.idea/
.vscode/
!.vscode/extensions.json
*.sublime-workspace
